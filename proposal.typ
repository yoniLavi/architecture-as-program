// Document setup
#set document(
  title: "Architecture as Program: Capability-Injected, Model-Driven Software Development in the Age of AI Agents",
  author: ("Yoni Lavi"),
  date: datetime(year: 2026, month: 7, day: 14),
)
#set page(margin: 2.5cm, numbering: "1")
#set text(font: "New Computer Modern", size: 11pt)
#set heading(numbering: "1.1")
#set par(justify: true)

// Title block
#align(center)[
  #text(size: 16pt, weight: "bold")[
    Architecture as Program: Capability-Injected, Model-Driven Software Development in the Age of AI Agents
  ]
]

#align(center)[
  #text(size: 12pt)[Yoni Lavi] \
  #text(size: 10pt, style: "italic")[Codeliance]
]

#align(center)[
  #text(size: 11pt)[First circulated June 2026 · Last revised July 2026]
]

#v(1em)

#align(center)[
  #block(width: 85%, stroke: 0.5pt + luma(180), inset: 1em, radius: 3pt)[
    #set text(size: 9pt)
    #set par(justify: true)
    *Note on process.* This proposal was developed collaboratively with Claude (Anthropic), which served as primary drafter under the author's direction. The architectural vision and synthesis are the author's; the literature survey, formal framing, and prose were produced by the AI and verified against primary sources. This transparent accounting reflects the proposal's own thesis: that the interesting artifact is the _intent_, not the implementation, and that honest attribution of AI contribution is preferable to ambiguity.
  ]
]

#v(1em)

#block(width: 100%, inset: (x: 2em))[
  #set text(size: 10pt)
  #set par(justify: true)
  *Abstract.* AI coding agents have removed the thirty-year obstacle to graph-based code representations: the human preference for text. This proposal describes a development model in which the primary artifact is a _signal graph_ (a functional reactive program with explicitly typed capability boundaries) that serves simultaneously as architecture model, security policy, and source of truth. Implementation code is typically a compiled artifact generated by AI agents within capability-restricted sandboxes; existing code can be incorporated as opaque nodes with explicit capability signatures, giving a migration path that does not demand rewriting up front. We synthesise functional reactive programming, object-capability security, and trust-annotated information flow into a single architectural substrate, and argue that this synthesis is newly practical due to the convergence of agent-authored code, lightweight sandboxing (the WASM Component Model, BEAM), and capability hardware (CHERI). The proposal presents a three-phase research agenda, from core demonstrator through formal verification, and identifies the open problems that must be resolved to deliver on its claims.
]

#v(1em)

= The window that just opened <sec:window>

For thirty years, a small community of researchers pursued an idea that working software developers consistently rejected: that source code should be a structured graph, not text in files. Projectional editors @jetbrains_mps_2025, structure editors @simonyi_intentional_2006, model-driven architecture @omg_mda_2014: the history of these efforts is a history of elegant ideas that foundered on a single obstacle: programmers prefer text editors, value syntactic flexibility for half-formed thoughts, and resist representations that constrain expression before intent is clear.

That obstacle has moved.

The rapid adoption of AI coding agents (Claude Code @anthropic_claude_code_2025, Codex @openai_codex_2025, Cursor @anysphere_cursor_2025, Windsurf @codeium_windsurf_2025) has introduced a new primary author of implementation code that does not _require_ the specific affordances of text editing (syntactic flexibility, tolerance of temporarily invalid states) that motivated human resistance to structured representations. What agents benefit from is semantically rich, machine-readable representations of intent that prevent entire categories of error before generation begins. The graph representation that human developers rejected may be precisely what agent-authored software requires.

Moreover, early experience with unconstrained AI coding agents suggests recurring quality problems @gitclear_ai_2024. Agents can produce code that is locally correct but globally incoherent — architectural drift, duplicate abstractions, convention violations, unintended data flows — because no structural constraints exist to make these violations impossible. A graph-based representation would address this at the source, replacing implicit conventions with typed, machine-readable constraints that agents could not violate. Recent work on LLM code generation within the Hazel typed-hole environment @blinn_statically_2024 provides direct empirical evidence: providing agents with rich static context from the type system substantially improves generation quality.

At the same time, a parallel development has made this timely: the emergence of spec-driven development frameworks (OpenSpec @fission_openspec_2025, GitHub's Spec Kit @github_spec_kit_2025, AWS's Kiro @aws_kiro_2025) that capture developer intent as structured artifacts before any code is written. These frameworks implicitly acknowledge that when AI agents write the code, the _intent_, not the generated implementation, is the artifact that matters, and that agents without structured constraints produce results that require disproportionate review effort.

This proposal argues that these two trends (agents as primary code authors, and structured intent capture as primary human activity) converge on a new software development paradigm. In this paradigm, a _signal graph_ (a program written in a functional reactive style with explicitly typed capability boundaries) is simultaneously the architecture model, the security policy, and the program. Code is a compiled artifact derived from it. Security would be a structural property of the graph, not an aspiration enforced by review. The signal graph defines _computation topology_ — what components exist, how data flows between them, and what authority each holds — not deployment topology (the domain of infrastructure-as-code tools such as Terraform or Pulumi).

= The central thesis <sec:thesis>

We propose a development model with four interlocking properties.

*The signal graph as source of truth.* The primary artifact that humans author, review, and version-control is a functional reactive program (FRP — see @sec:frp for the paradigm and its history): a directed graph in which each node is a function from time-varying typed inputs to time-varying typed outputs, with any effects mediated through explicitly declared capability handles. This graph is simultaneously the architecture model, the security policy, and the program. It _is_ the implementation at the level of abstraction humans author and review; the code inside each node is a generated artifact derived from it.

*Capabilities as declared requirements.* Nodes have no ambient authority: side effects (database access, network calls, LLM invocations, event emission) are not capabilities that code can reach for from the surrounding environment. They are typed capability handles declared separately from data inputs using a `with` clause — for example, a node with signature `(OrderRequest) → OrderConfirmation with DBHandle<'orders', read-write>, EventEmitter<'order-events'>` can read and write orders and emit order events, and nothing else, because no other mechanism exists within its scope. A node with no `with` clause is a pure function of its inputs in the conventional sense.

*Code as compiled artifact.* AI agents generate the implementation of each node to satisfy the behavioural contracts encoded in the graph's type signatures. The imperative code inside each node is an implementation detail, analogous to compiled bytecode. It can be regenerated, refactored, or rewritten without changing the system's meaning, provided it satisfies its contracts — an interchangeability claim that holds up to the expressiveness of those contracts, a limit examined in Technical Note A. Humans review graph transformations; they do not routinely review generated code.

*Security by construction.* Because capabilities are injected and the type system propagates trust annotations, security properties would be structural invariants of the graph rather than aspirations enforced by code inspection. The strength of the resulting guarantees varies by vulnerability class and is worth naming up front. Injection attacks that depend on untrusted input reaching an interpreter in executable form would be statically ill-typed in a sound realisation. Privilege escalation — a node acquiring capabilities it was not given — would be prevented by construction, because the graph would be the complete and sole description of the capability distribution. Prompt injection and confused-deputy patterns would be structurally attenuated through capability scoping and trust propagation rather than wholly eliminated; the precise strength of each guarantee, together with its schema-design prerequisites, is detailed in @sec:security.

= Functional reactive programming as the conceptual core <sec:frp>

The signal graph model is not a novel metaphor. It is a direct application of _functional reactive programming_ (FRP), a paradigm with a nearly thirty-year research history, here elevated from a UI programming technique to a whole-system architectural substrate.

== FRP: a brief account <sec:frp-brief>

FRP was introduced by Elliott and Hudak in their 1997 paper _Functional Reactive Animation_ @elliott_functional_1997, which modelled interactive animations as pure functions over continuous time. The central abstractions are _behaviours_ (values that vary continuously over time) and _events_ (discrete occurrences at points in time). Programs are expressed as compositions of these abstractions, without explicit state mutation or callback registration. The semantics are denotational: a behaviour is literally a function `Time → Value`, giving the paradigm a clean mathematical foundation that supports equational reasoning unavailable in imperative or callback-driven styles.

Subsequent work refined the model. Wan and Hudak's _Functional Reactive Programming from First Principles_ @wan_functional_2000 gave the paradigm a rigorous stream-based semantics, characterising the conditions under which a sampled implementation is faithful to the continuous denotation. Building on Hughes's arrow abstraction @hughes_generalising_2000, Nilsson, Courtney, and Peterson then developed _arrowized FRP_ @nilsson_functional_2002, which makes _signal functions_ (rather than signals themselves) the primary composable unit. This restriction prevents certain classes of space and time leaks and, more importantly for our purposes, makes the interface of each signal function explicit in its type. A signal function with signature `SF a b` transforms a signal of `a` values into a signal of `b` values. This is the formal object we extend with capability annotation.

The most prominent early realisation of FRP principles for browser UIs was Elm @czaplicki_elm_2012, which enforced purity strictly, made all effects explicit and managed by the runtime, and avoided runtime exceptions in practice. Elm later moved away from explicit FRP, but the design properties it demonstrated remain instructive. In the JavaScript ecosystem, the FRP lineage continued through RxJS @rxjs_2025 (observable stream composition) and Cycle.js @cyclejs_2025, which structures entire applications as pure functions from input streams to output streams with all side effects handled by external drivers. Cycle.js is particularly relevant: a component not given a DOM driver or an HTTP driver simply cannot perform those effects, a discipline close to our proposal's capability injection. The gap is the absence of fine-grained capability typing and trust propagation, not the basic architectural shape.

A parallel tradition of graph-based reactive programming exists in the synchronous dataflow languages (Lustre @halbwachs_synchronous_1991, Esterel @berry_esterel_1992, and Signal @benveniste_signal_1991), developed primarily for safety-critical embedded systems. These languages compile reactive signal graphs to deterministic, formally verifiable code and have decades of deployment in avionics and nuclear instrumentation. An adjacent industrial tradition, flow-based programming @morrison_flow-based_1994, anticipated the componentised form of the idea: applications assembled as networks of black-box components exchanging data across named ports, with the network definition maintained as an artifact distinct from component code — though without static typing of the network, and with no notion of capability or trust. At the commercial end of this tradition, LabVIEW @ni_labview_2025 and Simulink @mathworks_simulink_2025 are the most widely deployed graph-based programming environments, validating the "graph as program" concept at scale while also illustrating the UX challenges (version control difficulty, visual clutter, loss of context at scale) that inform the graph-scale comprehension problem discussed in Technical Note A. The synchronous dataflow languages' formal verification track record is directly relevant to Phase 3 of our research agenda, though their focus on synchronous, clock-driven execution differs from the asynchronous, event-driven model our proposal targets.

More recently, the FRP research community has explored _differential_ computation, evaluating only the graph nodes affected by a change. Differential dataflow @mcsherry_differential_2013, as implemented in Materialize @materialize_2025 and the DBSP framework @budiu_dbsp_2023, demonstrates that this is practical at database scale. Our proposal's claim that the signal graph can serve as a production substrate, beyond a development-time abstraction, depends on this line of work.

*A concrete illustration.* To motivate the extensions developed in @sec:frp-architecture, consider a node that processes user-submitted text and passes it to an LLM with tool-calling access. In a conventional system, this is dangerous: if the submitted text contains adversarial instructions, the LLM may execute them using its tools. The vulnerability is not a bug in any individual component; it is an architectural property: the unintended flow from untrusted input to a privileged executor.

In the signal graph, the same scenario is expressed as two nodes. The first, `UserInputHandler`, has signature:

```
UserInputHandler : (HTTPRequest<'POST', 'user:message'>) → Untrusted<UserMessage>
```

The second, `LLMOrchestrator`, has signature:

```
LLMOrchestrator : (SanitisedPrompt) → AgentResponse
  with LLMClient<[respond, lookup]>
```

A direct wiring from `UserInputHandler`'s output to `LLMOrchestrator`'s input is a _type error_: `Untrusted<UserMessage>` does not match `SanitisedPrompt`. The graph cannot be assembled without an explicit node that transforms `Untrusted<UserMessage>` into `SanitisedPrompt` — a node whose existence is visible in the architecture, whose implementation is subject to contract verification, and whose presence is required by the type system rather than by a policy document. In a well-typed realisation of this model, the prompt injection vulnerability would be _ill-typed_: no well-typed graph could express it. The type system design that delivers this guarantee is the central obligation of Phase 1 (@sec:phase1); the example illustrates the target property, not a proven result.

#figure(
  image("dist/diagrams/typed-wiring.svg", width: 80%),
  caption: [The direct wiring of the two nodes above is structurally rejected: the output type `Untrusted<UserMessage>` does not inhabit the input type `SanitisedPrompt`. An explicit sanitisation node — not shown — would be required by the type system to close the gap. Security, here, would be a property of graph shape.],
) <fig:typed-wiring>

== Extending FRP: capability annotation and trust tainting <sec:frp-architecture>

The step from FRP as a UI technique to FRP as a whole-system architectural model requires two extensions that the existing literature does not fully address.

The first is _capability annotation_. Standard FRP treats effects as values managed by the runtime, but does not give them a fine-grained type structure that distinguishes, say, a read-only database handle from a read-write one, or a sanitised string from an untrusted one. The object-capability model @miller_robust_2006 provides the missing ingredient: capabilities are unforgeable typed references whose possession is the proof of authorisation. Combining FRP's signal graph semantics with the object-capability model's typed authority gives us signal graphs in which data-flow and capability-flow are both first-class, typed, and statically checkable.

The second is _trust tainting_. Data entering the graph from untrusted sources (user input, third-party API responses, LLM outputs) carries a type marker that propagates through signal transformations until it passes through an explicitly designated sanitisation node. This is analogous to taint tracking as studied in information-flow security @sabelfeld_language-based_2003, and specifically to the labelled-IO approach demonstrated by practical information-flow control libraries such as LIO @stefan_flexible_2011, but expressed as ordinary type-level propagation rather than a separate analysis. A node that accepts `Untrusted<string>` and a node that accepts `LLMClient<[respond, lookup]>` cannot be directly wired; the type system would prevent the combination. The graph topology would enforce the security property without separate analysis.

== Time as a structural dimension <sec:time>

In a conventional program, time is implicit: state changes in place, and its history is lost unless explicitly logged. In the proposed signal graph model, under the purity guarantees the runtime would enforce, time would be structural: every signal would carry a history, and the system's behaviour at any point would be a pure function of its input signals up to that point. This has immediate practical consequences.

Proposing a change to the system would mean forking the signal graph's timeline. An agent would explore the fork, observing projected effects on downstream signals. If the exploration is satisfactory, the fork is merged into the main timeline; if not, it is discarded with no cleanup cost, because the fork is a value, not a mutation. The human review step would not be a diff of two static models but a _behavioural comparison of two timelines_, including the agent's exploratory history and the projected downstream effects on dependent signals.

In production, this structural temporality would double as observability infrastructure. Every crossing of a capability boundary (every database read, network call, or LLM invocation) would be a typed, observable event. A structured log of these events would constitute a record of the system's inputs (subject to the fidelity limitations discussed in Technical Note A). Given that log and a deterministic signal graph, the system's behaviour at any past point would be substantially reproducible. Debugging a production failure would mean replaying the event log in the development environment, reconstructing the timeline, and forking the failure point. The replay of the production event log would provide a substantial basis for regression testing; authoring it would not be a separate step.

= Prior art: the pieces that exist <sec:prior-art>

Each element of this proposal has been explored independently; the convergence that makes their synthesis newly practical is discussed in @sec:why-now. To our knowledge, no existing system simultaneously provides graph-level capability analysis, trust-propagated type checking across component boundaries, and AI-generated implementations executing within capability-restricted sandboxes; the security-by-construction property emerges from their combination, not from any element alone.

== Model-driven architecture and its lessons

The Object Management Group's Model-Driven Architecture (MDA) programme @omg_mda_2014, launched in 2001, pursued a superficially similar vision: models as the primary artifact, with code generated from them. MDA foundered on the round-trip engineering problem: models and generated code diverged as soon as developers edited the code directly, and keeping the two synchronised became more expensive than maintaining the code alone. Our proposal avoids this failure mode structurally. The signal graph is not a model _of_ the code; it _is_ the program, at the level of abstraction humans review. Implementation code inside nodes is a generated artifact that can be regenerated without invalidating the graph, because the graph's type signatures and contracts (not the implementation) are the stable interface. There is no round-trip problem because there is no return trip: humans do not edit generated node implementations.

== Architectural modelling: C4 and its ecosystem <sec:c4>

The C4 model @brown_c4_2018 provides a hierarchical approach to software architecture visualisation across four levels of abstraction: Context, Containers, Components, and Code. It has become the most widely adopted lightweight architecture diagramming approach, supported by tools including Structurizr, LikeC4, IcePanel, and Mermaid.

Two recent developments extend C4 toward the role we envision. LikeC4 @likec4_2025 provides an MCP server that exposes the architecture model to AI agents as a queryable knowledge base, transforming static diagrams into an interactive substrate that agents can interrogate. Some practitioners, including the author @lavi_c4_skill_2025, have begun using C4 models as "executable context" for agents, maintaining the architecture model in the repository as the source of truth that constrains agent behaviour.

C4's limitation for our purposes is that it is a _communication_ model, not a _constraint_ model. It describes architecture but does not enforce it. Our proposal replaces the C4 model with a typed signal graph that both describes and enforces; the diagram and the program are the same artifact.

== Effect systems and purity: Haskell, Idris, and beyond <sec:effects>

Haskell demonstrated that purity-by-default with explicit effects is practical for real software @peyton_jones_haskell_2003. The IO monad makes side effects visible in function signatures: a function of type `a → b` is guaranteed pure, while `a → IO b` declares that it performs effects. More recent work (algebraic effect systems as in Koka @leijen_type_2017 and Frank @lindley_be_2017, and dependent type systems as in Idris 2 @brady_idris_2021) makes this more expressive: effects can be parameterised, composed, and reasoned about as first-class values.

Roc @feldman_roc_2025 takes a different but relevant approach: it is a pure functional language in which all side effects are provided by an interchangeable "platform" that the application cannot bypass. A Roc application that targets a web server platform can handle HTTP requests but cannot access the filesystem; the platform boundary is the capability boundary. This is the closest existing language-level analogue to our proposal's per-node capability injection, though Roc operates at the whole-application level rather than per-component.

A more recent line of language research makes capabilities themselves the type-level currency for effects. Effekt @brachthauser_effects_2020 treats effect operations as _capabilities_: a function can perform only those effects for which it has been passed a capability value, and the type system tracks which capabilities are in scope — an approach its authors call lightweight effect polymorphism. Scala 3's capture checking @boruch-gruszecki_capturing_2023 generalises the idea: types record which capabilities a value _captures_, so a function's signature exposes the authority it may exercise. Both demonstrate that capability tracking can be made ergonomic inside a general-purpose type system, at function granularity within a single program.

Our proposal applies this insight at the _architectural component level_ rather than the language type level. The granularity is coarser (components rather than functions), and the enforcement mechanism is the runtime rather than the compiler. But the principle is identical: effects are declared in signatures, not acquired from ambient context. The formal verification obligation described in @sec:agenda is, in part, the obligation to show that this coarser enforcement is sufficient for the security properties we claim.

== Content-addressed code: Unison <sec:unison>

Unison @chiusano_unison_2025 takes the position that text files are the wrong storage substrate for code from the outset. Definitions are identified by a hash of their abstract syntax tree rather than by name; the codebase is an append-only database of typed ASTs rather than a directory of files. The consequences are practically significant: the codebase is always in a type-checked state, incremental compilation is perfect (the same definition is never compiled twice), and semantically-aware version control eliminates entire classes of merge conflict. Unison also provides an algebraic effect system ("abilities") in which functions declare required effects in their type signatures, enforced by the type system, such that a program can only perform effects for which it has been explicitly given an ability. Version 1.0 was released in 2025, demonstrating that content-addressed, database-backed code storage is production-viable.

A related experiment is Darklang @biggar_darklang_2019, which attempted a holistic programming environment where code, editor, and infrastructure were unified: deployable directly from the structured editor with no separate build or deploy step. Darklang demonstrated genuine developer productivity gains for simple backend services but struggled with the adoption costs of a fully proprietary environment and the difficulty of scaling a bespoke infrastructure layer. Its trajectory is instructive: the vision of a unified, structured development substrate is compelling, but adoption requires either an incremental migration path from existing tools or such overwhelming productivity advantages that developers accept the switching cost. Our proposal learns from this by treating the signal graph as a layer above existing runtimes (WASM, BEAM) rather than a replacement for them.

In a related vein, the Nix package manager @dolstra_purely_2006 and its sibling Guix demonstrate that content-addressed, capability-restricted computation is viable at scale in a different domain: software builds. Nix derivations are pure functions from declared inputs to outputs, with no ambient access to the network or filesystem during builds. The Nix store is a content-addressed database of build artifacts. The result is hermetic, reproducible builds across millions of packages. While Nix operates at the build level rather than the runtime level, it provides a large-scale existence proof that the capability-restricted, content-addressed computational model our proposal envisions is practical.

Our proposal's treatment of node implementations as compiled artifacts derived from the signal graph is architecturally consistent with Unison's model at the implementation layer. The signal graph itself, however, operates at a different level: Unison's composition model addresses functions and libraries, while ours addresses explicitly wired capability topology. Similarly, Unison's ability system tracks _what category of effect_ a function performs but does not enforce fine-grained authority boundaries — the distinction between a handle scoped to a specific database versus ambient database access, or the propagation of `Untrusted<T>` trust labels. The two approaches are complementary: Unison addresses code storage and effect declaration; our proposal addresses architectural wiring and trust propagation.

== Live programming with typed holes: Hazel <sec:hazel>

Hazel @omar_hazelnut_2017 @omar_live_2019 is a live functional programming environment built around the principle that every editor state should be statically and dynamically meaningful, even when the program is incomplete. It achieves this through _typed holes_: missing or type-inconsistent expressions are wrapped in holes that carry type information and, in the dynamic semantics, propagate as opaque values through evaluation. The result is that feedback (type errors, live outputs, hole closure information) is available continuously during editing rather than only when a program is complete.

Hazel's relevance to the present proposal is twofold. First, it provides semantic foundations for the development workflow described in @sec:workflow: an agent proposing a graph transformation will, during the proposal phase, produce a partially-complete graph containing unfilled node signatures. Hazel's hole calculus demonstrates that such partial states can be given well-defined types and evaluated meaningfully, supporting the "project downstream effects" step of the workflow without requiring the entire graph to be complete before any inference is possible. Second, a 2024 paper from the Hazel group @blinn_statically_2024 integrates LLM code generation directly into the typed-hole environment, finding that providing the LLM with static context from the hole's type and typing environment substantially improves generation quality. This is a direct empirical precedent for the claim in @sec:workflow that agents generating node implementations benefit from the semantically rich context that the signal graph's type signatures provide.

== Process isolation and message passing: Erlang/BEAM <sec:beam>

The BEAM virtual machine @armstrong_making_2003, underlying Erlang and Elixir, provides the closest existing model to the runtime we envision. BEAM processes are lightweight, fully isolated (no shared memory), and communicate exclusively by message passing. A process cannot reach into another process's state or access global mutable resources. The "let it crash" philosophy, where individual processes fail and are restarted without system-wide impact, is a direct consequence of isolation.

More broadly, the signal graph's model of isolated components communicating by typed messages is an instance of the actor model @hewitt_universal_1973, and the resemblance to BEAM processes is not coincidental. However, the actor model as typically realised (Erlang, Akka, Orleans) leaves two things implicit: the set of messages an actor may _receive_ is visible in its interface, but the set of external resources it may _access_ is not, and the wiring between actors is determined at runtime by message sends rather than declared statically.

The closest existing prior art to our proposal's capability-typed composition is Pony @clebsch_deny_2015, which integrates _reference capabilities_ (iso, val, ref, box, trn, tag) directly into the actor-model type system and enforces data-race freedom at compile time. Pony demonstrates that capability annotations can be made first-class in a practical actor language and that the type system can carry them through composition — a validation of the general approach on which this proposal builds. The point of divergence is the target property: Pony's capabilities govern memory access patterns (aliasing, mutability), whereas the signal graph's capabilities govern external authority (database access, network calls, LLM invocation). The signal graph thus lifts Pony's insight from the memory-reference level to the architectural level, and pairs it with trust propagation and an explicit wiring graph.

Our proposal extends the actor/BEAM insight in two ways. First, we make the message-passing interfaces typed and capability-aware: a component's signature declares not just what data it accepts but what capability objects it requires. Second, we make the wiring of components explicit in the signal graph rather than implicit in application code. The graph serves as a declarative analogue of the supervision tree, expressing component wiring at a level humans can reason about, though failure handling and restart policies require additional specification.

== Capability-based security <sec:capabilities>

The object-capability model @miller_robust_2006 @goos_paradigm_2003 holds that access to a resource requires possession of an unforgeable reference to that resource. Rather than checking permissions against an access control list, a capability system makes the capability itself the proof of authorisation. This model has been implemented at every level of the computing stack: in programming languages (the E language @miller_robust_2006, Google's Caja @google_caja_2012), in operating systems (Capsicum @watson_capsicum_2010, seL4 @klein_sel4_2009), and in runtime environments (Deno @deno_2025, WebAssembly/WASI).

The most directly relevant prior work for our purposes is the treatment of capability-passing in _distributed_ systems, where the additional concern is that network reachability can itself constitute ambient authority. Miller's E language @miller_robust_2006 addressed this by mediating all inter-object communication through explicit references passed through the object graph; no ambient network or global namespace is available. The Agoric platform @agoric_hardened_2023 extends this model to JavaScript through Hardened JavaScript (SES, Secure EcmaScript), demonstrating that object-capability (ocap) discipline is achievable in a mainstream language without requiring a new runtime. Stiegler's _An Introduction to E and the Distributed Object-Capability Model_ @stiegler_introduction_2010 provides an accessible treatment of the distributed case. Our signal graph's explicit edge wiring is the architectural-level analogue of E's reference passing: a node that is not wired to an external network capability handle has no mechanism for external communication, regardless of the network services that exist at the operating system level.

More recently, AWS's Cedar @cutler_cedar_2024 provides a formally verified authorisation policy language with a capability-aware structure, demonstrating that fine-grained, analysable authority models are viable in production cloud infrastructure. Cedar's approach (expressing policies as analysable programs rather than opaque access-control list (ACL) tables) is philosophically aligned with our proposal's treatment of capability wiring as a typed, reviewable artifact.

The combination of capability-based security with FRP's signal graph model is, to our knowledge, novel as a whole-system architectural substrate. Existing capability systems enforce authority restrictions at runtime; existing FRP systems enforce dataflow discipline at the type level. The proposed synthesis would enforce both simultaneously, making the two disciplines mutually reinforcing rather than independently applied.

== CHERI: capabilities in hardware <sec:cheri>

Capability Hardware Enhanced RISC Instructions (CHERI) @watson_cheri_2015 implements capability-based memory protection directly in hardware. On a CHERI processor, every pointer is a capability: a hardware-protected value carrying an address, bounds, permissions, and a tag bit checked on every memory operation. Capability forgery (via buffer overflow, type confusion, or integer-to-pointer cast) causes a hardware trap. No software guard is needed.

CHERI is reaching commercial maturity. Arm's Morello chip @arm_morello_2023 demonstrated CHERI on AArch64. Microsoft's CHERIoT @amar_cheriot_2023 adapted CHERI to RISC-V for embedded devices. Codasip @codasip_cheri_2024 has released commercial CHERI RISC-V processor IP. SCI Semiconductor announced the ICENI family, based on CHERIoT-Ibex on RISC-V, in 2024 @sci_iceni_2024 and reached first silicon in March 2026 @sci_first_silicon_2026 — which the company describes as the first commercial CHERI implementation in silicon. The CHERI Alliance @cheri_alliance_2024, with Google as a founding member, was established in 2024 to coordinate adoption.

Three CHERI properties matter for our proposal. First, _unforgeable capabilities_: a component that does not possess a capability to a memory region cannot acquire one through any means the processor permits. Second, _fine-grained compartmentalisation_: capabilities can be scoped to individual allocations, enabling component isolation within a single process at hardware speed. Third, _demonstrated low porting cost_: a 2021 study ported six million lines of C and C++ (KDE, Qt, X11) to CHERI with changes to 0.026% of source lines @capabilities_limited_cheri_2021. For AI-generated code targeting a CHERI-aware runtime from scratch, the porting cost is zero (assuming the runtime itself has been ported, a one-time engineering effort).

== Lightweight sandboxing <sec:sandboxing>

The practical feasibility of per-component isolation has improved dramatically. WebAssembly (WASM) provides capability-based isolation with near-native performance across languages @haas_bringing_2017. Two layers on top of it matter here, and it is worth keeping them distinct, because the substrate we depend on is the more durable of the two. The _WASM Component Model_ @w3c_component_model_2024 is the interface-type layer: components declare their imports and exports as typed interfaces (defined in the WIT interface-definition language), and the runtime links them with type-checked bindings. The _WebAssembly System Interface_ (WASI) is a standard _library_ of such interfaces — for filesystems, clocks, sockets, and the like — built on top of the Component Model. This distinction is directly relevant to the signal graph, and the correspondence is close enough to exploit directly: the proof-of-concept tier described in @sec:phase1 expresses each capability kind as its own WIT interface, so that a node's `with` clause _is_ its component's import list and the runtime boundary is a realisation of the node's typed signature rather than a byte protocol beneath it.

WASI is itself a worked precedent for the pattern this proposal generalises. Its I/O model is explicitly capability-based: a component receives handles to the resources it may access at instantiation, with no ambient access to the host environment, and each such resource — `wasi:filesystem`, `wasi:clocks`, `wasi:sockets` — is a typed interface granted explicitly. That is structurally the same object as our `DBHandle<'knowledge-base', read>` or `LLMClient<inference>`, applied to host-system resources rather than application-level ones. The signal graph does not adopt WASI so much as it recognises WASI's capability interfaces as one instance of capability-as-interface, and extends the same discipline to the application capabilities WASI does not model.

The layering also settles a natural question about longevity, since WASI's interfaces are still in motion. WASI 0.1 (the POSIX-flavoured `wasm32-wasip1` snapshot) is legacy; WASI 0.2 rebased the standard onto the Component Model; WASI 0.3 @wasi_030_2026, ratified in mid-2026, added native async and _removed_ the `wasi:io` package entirely, folding it into the Component Model's canonical ABI; a long-term-stable WASI 1.0 is expected to follow @wasi_roadmap_2026. The interfaces churn — an artifact importing `wasi:io/streams` under 0.2 must migrate for 0.3 — but every version rebases onto the same underlying layer, the Component Model, which is the part advancing toward standardisation. The design consequence for this proposal is to bet on the substrate, not on any WASI snapshot: the proof-of-concept accordingly defines its own WIT capability interfaces rather than importing WASI's, and is untouched by that interface churn. Where a node genuinely needs a host resource, the corresponding WASI interface (at whatever version) is the right thing to grant it — as a capability, exactly like any other.

Where the guarantee lives is a related subtlety that the same layering clarifies, and it matters in practice. A `wasm32-wasip1` module still _imports_ WASI functions and is confined only because the host supplies an empty context behind them; confinement is then a property of the host's configuration. A component built without a WASI adapter does not import those functions at all, so its confinement is a property of the artifact — the distinction the @sec:phase1 tier is built to make, and the reason it targets a bare component rather than a WASI snapshot. Beyond WASM, Pydantic's Monty @pydantic_monty_2026, a minimal Python interpreter written in Rust, achieves microsecond-scale startup with complete host isolation by default; BEAM processes start in single-digit microseconds; and CHERIoT demonstrates hardware-enforced compartmentalisation with negligible overhead on resource-constrained devices.

CHERI hardware provides a backstop for software sandboxes: even if a sandbox implementation contains a memory safety bug, the hardware prevents capability forgery at the memory level. The result is two complementary enforcement layers (software sandbox and hardware capability), providing overlapping coverage with distinct failure modes.

== Durable execution and replay <sec:durable>

The snapshotting, replay, and time-travel debugging properties described in @sec:time have significant prior art in the durable execution paradigm. Temporal @temporal_2025, Restate @restate_2025, and Azure Durable Functions @microsoft_durable_functions_2025 provide production-grade infrastructure for persisting workflow state, replaying execution from event logs, and resuming after failure. These systems demonstrate that deterministic replay from logged events is practical at scale.

A persistent challenge for durable execution frameworks is nondeterministic interleaving: replay must reproduce the same ordering of concurrent operations that occurred in the original execution, or the replayed state diverges. Temporal addresses this by requiring orchestrator code to be deterministic; Restate uses a journal that records the outcome of each operation. Both approaches work but impose constraints on application code. The signal graph's pure, deterministic propagation semantics would avoid most of this class of problem at the inter-node level: signal propagation order would be determined by graph topology rather than by runtime scheduling, with one residual exception — the merge order at fan-in points, where several upstream nodes feed a single consumer — discussed under replay fidelity in Technical Note A. Replay fidelity for nodes with internal concurrency or timing dependencies remains an open question (see Technical Note A).

Our proposal's replay model also differs in granularity and scope: durable execution frameworks replay at the _workflow step_ level, while the signal graph would replay at _capability boundary crossings_, which would provide a finer-grained and more complete record of system inputs. The signal graph also integrates replay with the type system's trust and capability annotations, enabling the replay infrastructure to enforce the same security properties as the live system. These production frameworks validate that event sourcing and deterministic replay are well-understood engineering.

== Spec-driven development <sec:sdd>

The SDD movement, represented by OpenSpec @fission_openspec_2025, GitHub's Spec Kit @github_spec_kit_2025, and AWS's Kiro @aws_kiro_2025, addresses the problem that AI coding agents are unpredictable when requirements live only in chat history. These frameworks create structured, versioned specification artifacts that persist in the repository and provide agents a stable context.

Codespeak @breslav_codespeak_2025, created by Kotlin designer Andrey Breslav, takes this further: developers maintain plain-English specifications that compile via LLM to Python, Go, or TypeScript, treating implementation code as a generated artifact. The framing — "maintain specs, not code" — is close to our proposal's treatment of node implementations as compiled artifacts derived from the signal graph. However, Codespeak's specifications are untyped prose without capability annotations or trust propagation, so the security-by-construction properties the signal graph provides are outside its scope.

Current SDD frameworks, including Codespeak, treat their spec artifacts and the architecture model as separate concerns. Their outputs are prose documents or natural-language specifications with limited formal structure. Our proposal argues that as these frameworks mature, their output should converge with the signal graph: a proposed change is a transformation of the typed graph, not a separate markdown document. The distinction between "spec" and "architecture" dissolves when the graph is both.

A parallel development at a different level of technical sophistication supports this trajectory. Visual workflow automation platforms (Zapier @zapier_2025, Make.com @make_2025, and n8n @n8n_2025) have achieved mass adoption by letting non-developers build systems as directed graphs of triggers, actions, and conditional branches. n8n's AI Workflow Builder already implements the core interaction loop our proposal envisions at a higher level of abstraction: a user describes intent in natural language, the AI generates a graph (represented as JSON), and the user reviews and refines the result visually. At the developer-facing end of the same spectrum, agent-orchestration frameworks such as LangGraph @langchain_langgraph_2025 structure LLM applications themselves as explicit graphs of state transitions — a further sign that graph-shaped system definitions are becoming the working representation for AI-era software. These platforms validate the appetite for graph-based system construction and the viability of AI-generated graph definitions. Their limitation is the absence of the properties this proposal requires: typed interfaces, capability restrictions, trust propagation, and formal security guarantees. As teams scale from internal automations to customer-facing AI agents, the gap between workflow automation and production-grade architectural rigour becomes acute. That trajectory converges on the kind of typed, capability-aware graph substrate we propose.

= The proposed system <sec:system>

== The signal graph <sec:signal-graph>

The primary artifact is a version-controlled, typed signal graph with the following structure.

*Nodes* are functions with explicit signatures. A node's signature has two parts: its _data inputs_ (typed signals from upstream nodes) and its _capability requirements_ (typed handles to external resources, declared with a `with` clause). The data inputs describe what the node transforms; the capability requirements describe what authority it has. This separation reflects a lifecycle distinction: data signals flow at runtime as the graph propagates, while capabilities are provisioned when the graph is instantiated. A node with no `with` clause is a pure function of its inputs; a node with a `with` clause is pure with respect to its inputs _given_ its handles, with all effects mediated through those handles.

_A note on the design choice._ An alternative design treats capabilities as ordinary typed parameters alongside data inputs, consistent with the object-capability model's principle that capabilities are just values. We separate them syntactically because the distinction between "what a node transforms" and "what authority it holds" serves different review concerns — architecture and security — and because the lifecycle difference is real: capabilities are bound at construction, data flows at invocation.

*Edges* are typed data connections between nodes. An edge from node A's output to node B's input is valid only if the types match. Edges carry data; capabilities are not wired through edges but provisioned via `with` clauses. This is a deliberate restriction. Object-capability systems in the E lineage permit dynamic delegation by passing capability references through messages, which is flexible but defeats static analysis of the authority topology. The signal graph trades that flexibility for a fully statically analysable capability distribution; the distributed extension of this trade-off is a separate problem discussed in Technical Note A. The `with` clauses collectively constitute the architecture's security policy, expressed as reviewable graph structure rather than prose. Because the graph's parameter list declares all external dependencies, swapping a production capability for a mock (replacing a live `DBHandle` with a test fixture, for example) requires only a change at the graph boundary — no node signature changes, since the `with` clause names a type, not a specific instance.

*Trust annotations* (the type-level markers introduced as trust tainting in @sec:frp-architecture) propagate through the graph. Data entering from untrusted sources carries a type marker, `Untrusted<T>`, that is preserved through transformations until explicitly discharged. In a well-typed realisation, the type system would prevent `Untrusted<T>` from reaching a node that accepts only `T`. Discharge is most effective when it is not merely a label removal but a _type transformation_: converting unstructured input into a constrained representation whose structure limits what downstream nodes can receive. The combination of trust propagation and structural typing is what delivers the security properties claimed in @sec:security.

An important open design question must be acknowledged here. The trust annotation scheme as described enforces the _local_ typing of individual nodes, but the full security guarantee requires that the _wiring_ also be checked; specifically, that a source classified as untrusted at the graph's edge cannot be connected to a node whose signature expects a clean `T`, bypassing the `Untrusted<T>` marker through a widening coercion. This is the standard coercion problem in information-flow type systems #cite(<sabelfeld_language-based_2003>, supplement: [§3]): local type correctness of nodes is necessary but not sufficient for noninterference; the type system must also enforce that the subtyping relation between `Untrusted<T>` and `T` is absent, or equivalently, that wiring compatibility checks are flow-sensitive with respect to trust levels.

Several solutions exist in the literature (most directly, treating trust levels as _security labels_ in the style of Jif @myers_decentralized_1997 or imposing a lattice structure on trust types with no upward coercion), and the proof-of-concept validator (@sec:phase1) now implements a first realisation of exactly this: trust levels form a two-point lattice (`Untrusted ⊑ Trusted`) in which `Untrusted<T>` is not a subtype of `T`, the wiring check is flow-sensitive with respect to trust (an edge is well-typed only if the source's trust label flows to the target's under the lattice order), and the only sanctioned upward move is an explicit discharge at a declared discharger. This subsumes what were previously two independent rules — edge typing and a standalone discharge check — into one lattice order applied uniformly to edges and to node bodies. The full design remains an open Phase 1 question: whether the minimal two-point lattice suffices or a graded (`Untrusted ⊑ Sanitised ⊑ Trusted`) or Jif-style decentralised-label model is needed, and — critically — a soundness argument that well-typed wiring implies noninterference (@sec:phase3), which the validator does not attempt. The proposal does not claim this problem is solved; it claims it is tractable, that a first realisation exists and runs on every build, and that the right place to solve it is in the type system, where the literature provides well-understood tools.

*Behavioural contracts* are attached to node signatures as pre- and postconditions. These are the specifications against which AI-generated implementations are verified, and the stable interface across which different implementations are interchangeable. The contract language is a Phase 1 design obligation: candidates range from refinement types in the Liquid Haskell tradition, through Dafny- or Ada-style declarative specifications, to lightweight examples-and-invariants in the QuickCheck tradition. Contracts are authored alongside node signatures — either by the developer at graph-review time, or proposed by the agent during intent capture and confirmed on review — and checked by a combination of static analysis (for properties the type system expresses directly) and property-based or contract-based testing at implementation time (for properties that require runtime evidence). The tractability trade-off between contract expressiveness and automatic verification is addressed in @sec:phase2.

=== A concrete graph <sec:concrete-graph>

The following pseudocode sketches an AI customer support agent as a signal graph. This scenario was chosen because it is a domain where the security properties of the signal graph model are most immediately visible: untrusted user input, LLM invocations with and without tool access, and fine-grained capability distinctions are all present. Unlike the simplified two-node illustration in @sec:frp-brief, this example shows a realistic pipeline with structured input parsing and content moderation. The `SanitisedPrompt` of that earlier sketch is expanded here into the refined type `ModeratedQuery`, with trust discharge decomposed across two dedicated nodes (parsing and moderation) rather than collapsed into a single transformation. No concrete syntax has been designed; this is illustrative of the kind of artifact a developer would author and review.

_Syntax conventions used below._ Types are written angle-bracketed: `T<...>`. Capability types are parameterised by scope: `DBHandle<'knowledge-base', read>` denotes a handle to the `knowledge-base` database in read mode, where `read`, `append`, and `read-write` denote modes in a permission lattice (a handle with a wider mode may be supplied where a narrower one is required: `read-write` covers both `read` and `append`, which are mutually incomparable). `LLMClient<inference>` is an inference-only LLM client (model access without tool-calling); `LLMClient<[lookup]>` grants a single named tool, `lookup`. Sum types may be written with named role labels, as in `ok: ModeratedQuery | violation: PolicyViolation | escalation: EscalationRequest` — the label on the left of the colon is an edge-addressable role, the type on the right is the variant's value type. An edge addresses a specific variant of an upstream node's output as `Node.role` (for example, `ModerateContent.ok` is the `ModeratedQuery`-valued output). These conventions stand in for a concrete syntax that Phase 1 must design; the value here is the structural shape, not the notation.

#raw(read("dist/graphs/customer-support.graph"), block: true)

The graph's parameter list declares its complete external dependencies: an inbound request, a database handle, two LLM clients with different permission levels, a response channel, and an event emitter. `CustomerRequest` is the domain entry type: a narrowed representation of HTTP POST traffic to `/customer/*` that can be produced either by a direct HTTP adaptor in a standalone deployment or by the `RouteRequest` dispatcher in the composed deployment shown below. Using a domain name for the entry type rather than a raw `HTTPRequest<...>` lets the same graph serve both deployments without signature churn. This list is the system's authority manifest. In production, these parameters are bound to real infrastructure; in testing, they are replaced with mocks or deterministic fixtures. Because the graph has a typed signature (its parameter list and output types), it can itself be used as a node in a larger graph. Hierarchical composition falls out naturally from the model. To make this concrete, the following graph sketches a platform that composes `CustomerSupport` (the graph above) alongside two other services:

#raw(read("dist/graphs/support-platform.graph"), block: true)

`CustomerSupport` is no longer nine nodes visible at this level; it is a single node with a typed signature. Its internal wiring — the trust zones, the graduated LLM access, the moderation routing — is encapsulated. At the platform level, the reviewer sees only what authority each service holds and how data flows between them. The platform's parameter list is the union of its sub-graphs' requirements: the `DBHandle`, `LLMClient`, and channel capabilities each appear exactly where they are needed. `BillingService` has read-write access to the billing database but no LLM access; internally it would be a conventional pure pipeline — input validation, policy checks, and DB transactions — with no reliance on LLM nondeterminism, and how its shape differs from `CustomerSupport` is itself visible at the composition level through the absence of LLM capabilities. `AgentDashboard` can read the knowledge base but cannot write to billing. These constraints are visible at a glance in the capability annotations.

The boundary output `ServiceOutcome` declared here is the aggregation of `CustomerSupport`'s internal terminal outputs — in this example, the disjoint union of `DeliveryConfirmation` (from the reply paths) and `EscalationTicket` (from the escalation path). The sub-graph has several terminal nodes of different types, and the composition requires a single boundary type; treating `ServiceOutcome` as that aggregated union is the simplest of several possible designs. The same type also serves as the boundary output of `AgentDashboard` and `BillingService`: at the platform level, `ServiceOutcome` is the shared outcome currency that `RecordAudit` consumes, and each service maps its own terminal types onto it. How multi-terminal sub-graphs should expose their outputs (as a union-typed boundary, as named multi-output ports, or via an explicit aggregation node inside the sub-graph) is a Phase 1 language-design question noted in Technical Note A.

Hierarchical composition also surfaces a design obligation the proposal does not claim to have solved. When `SupportPlatform` provisions `LLMClient<inference>` to `CustomerSupport`, the sub-graph's internal wiring must route that capability to the specific internal nodes that require it (`ParseMessage` and `ModerateContent`) and not to others. How a sub-graph exposes its internal capability requirements at its boundary — as a flat union reabsorbed by convention, as named capability slots, or via structural matching on capability types — is an open question noted in Technical Note A and part of the Phase 1 language design.

#figure(
  image("dist/graphs/support-platform.svg", width: 75%),
  caption: [The SupportPlatform composition graph. Each service node is a sub-graph (the nine-node CustomerSupport graph is now a single node). Capability annotations show the authority distribution at the platform level. The audit node collects outcomes from all services with append-only database access.],
) <fig:support-platform>

The `CustomerQuery` type is central to the security argument. `ParseMessage` does not merely strip the `Untrusted` wrapper from free text: it transforms unstructured input into a constrained representation — a classified intent (from a finite set of categories), extracted entity references, and bounded text fields — whose type limits what downstream nodes can receive. The raw message is consumed; downstream nodes never see it. `ModerateContent` then refines this further into a `ModeratedQuery` (the `.ok` variant of its output), a type distinct from `CustomerQuery` that records, at the type level, that a moderation check has occurred. Downstream nodes accept only `ModeratedQuery`, so a wiring that bypasses moderation is ill-typed; the type distinction makes the moderation step _load-bearing_ rather than advisory.

This is a stronger guarantee than trust annotation alone: a well-typed `ModeratedQuery` cannot carry arbitrary executable instructions in positions that flow to privileged nodes, _provided the schema is designed to exclude unbounded free text in those positions_. This proviso is essential and bears stressing: the framework provides the enforcement substrate (structural typing, capability separation, topological constraint), but eliminating prompt injection requires disciplined schema design within that substrate. A schema that retains a free-text field (most real schemas do, for the original question itself) still treats that field as adversarial at every point where it reaches an LLM-capable node. The defence is layered rather than absolute, but each layer is visible in the graph topology and enforceable by the type system.

#figure(
  image("dist/graphs/customer-support.svg", width: 85%),
  caption: [The CustomerSupport signal graph. Red shading marks the untrusted zone; green shading marks the structured region. Edges show data flow; capability requirements are annotated on each node. LLM access (blue) is graduated: inference-only for parsing and moderation, a single lookup tool for response generation.],
) <fig:customer-support>

This diagram is simultaneously the architecture model, the security policy, and the program. Several properties are visible at a glance, without reading any implementation code.

_Prompt injection is addressed through structural typing and topological constraint._ `ParseMessage` transforms raw input into a `CustomerQuery`, a constrained representation that discards the original free text. It is worth naming the trust placement this entails: the LLM is itself the trust-discharging component, since `ParseMessage` consumes `Untrusted<RawMessage>` and emits a non-`Untrusted` `CustomerQuery` on the strength of the LLM's classification. This trust is bounded by the LLM's capability shape (`LLMClient<inference>` grants model access without tool use) and by the schema of `CustomerQuery`, which constrains downstream exposure regardless of how the LLM is influenced. Both LLMs that process user input (`ParseMessage` and `ModerateContent`) have `LLMClient<inference>`: even if adversarial instructions influence their behaviour, they have no mechanism to act on them. The tool-capable LLM (`GenerateResponse`) receives only `ConversationContext` assembled from a `ModeratedQuery` and knowledge-base lookups — never raw user text. A direct path from untrusted input to a tool-capable LLM does not exist in the graph; in a sound realisation of the type system, it would be ill-typed. A subtlety must be acknowledged: capability restriction prevents the LLM from _acting_ on adversarial instructions, but not from being _influenced_ in its classification. An adversarially crafted message could cause `ParseMessage` to produce a `CustomerQuery` that misclassifies intent, routing the query to the wrong downstream path. The defence here is twofold: the attack surface is narrowed from arbitrary tool execution to incorrect routing within a typed pipeline (a qualitative reduction in severity), and the routing itself operates within the user's authorised scope — if downstream actions are bounded by the user's own credential-scoped capabilities (passed as a parameter at the graph boundary), a misrouted query can only trigger actions the user was authorised to perform. Separating user-level authorisation from node-level capability injection is a design consideration for Phase 1 and is listed as an open item in Technical Note A.

_Capability distribution is minimal and visible._ The `with` clauses are a complete manifest of the system's authority. `ReceiveMessage` is pure: it transforms a typed customer request into a domain message with no `with` clause. `ParseMessage` and `ModerateContent` have inference-only LLM access: enough to classify and evaluate text, not enough to act on instructions. `FetchContext` has read-only knowledge-base access. `GenerateResponse` has a scoped LLM client with a single lookup tool and read-only database access. Terminal nodes (`SendReply`, `NotifyUser`, `HandleLLMError`) each have only a session-scoped response channel. `EscalateToHuman` can emit to the support queue but cannot read or write any database; its `EscalationTicket` output is a published identifier that a containing graph may route onward (for auditing or agent-dashboard display) or discard, consistent with the pattern that every node produces a typed value whether or not the current composition consumes it. No node has more authority than its function requires.

_Conditional routing and error handling are explicit._ `ModerateContent` produces a three-way union: approved queries continue to the response pipeline as `ModeratedQuery`, policy violations are routed to user notification, and ambiguous cases are escalated to human agents. `GenerateResponse` returns `AgentResponse | LLMError`, with the error case routed to `HandleLLMError`. In both cases, the routing is a structural property of the graph, visible in the diagram and the pseudocode. The type system enforces that every declared variant has a downstream consumer, but it cannot force a node's implementation to emit the _correct_ variant for a given input — a `ModerateContent` that always emits `.ok` would be well-typed but behaviourally wrong. Variant-correctness is a behavioural-contract obligation, checked by the shallow-verification work of @sec:phase2.

For concision, this example also elides transient infrastructure failures: `ParseMessage` could fail to classify, and `FetchContext` could fail on knowledge-base read errors. A production-shape version of the graph would route those into error variants using the same variant-typing mechanism shown for `GenerateResponse`, with their own terminal handlers. This is a presentation choice in the example rather than a claim that such failures do not exist.

The precise syntax for conditional routing, fan-out, and error propagation is an open design question for the Phase 1 language design; see Technical Note A.

== The development workflow <sec:workflow>

+ *Intent capture.* A human describes a desired change in natural language. An SDD-style tool translates this into a proposed graph transformation: new nodes, modified `with` clauses, changed signatures or contracts. Existing SDD frameworks produce prose specifications; extending their output to _typed graph transformations_ is the step most load-bearing for the development workflow to cohere, and is a core Phase 1 deliverable (@sec:phase1).
+ *Graph review.* Humans review the diff as a visual graph change: new nodes highlighted, new capability requirements marked, trust boundary crossings flagged. This review is simultaneously an architecture review, a security review, and a design review. The reviewer is approving a typed program transformation, not reading prose.
+ *Implementation.* AI agents generate code for each new or modified node, targeting the capability-restricted runtime. Each node is implemented in isolation: the agent receives the node's signature, its contracts, and the types of its inputs and outputs. It has no visibility into adjacent nodes' implementations. The rationale for isolation is not that agents benefit from less context in general — prior work @blinn_statically_2024 shows the opposite — but that any cross-node dependency an implementation might introduce must flow through the typed signature, preserving the graph as the complete specification of inter-node interaction. Context the agent would otherwise glean from adjacent implementations (error conventions, retry shapes, shared assumptions) must be lifted into types or contracts to have effect; this is a discipline the graph enforces by construction.
+ *Verification.* Automated tooling confirms that implementations satisfy their contracts, that the assembled graph conforms to the declared types, and that no node exceeds its injected capabilities. Since the runtime enforces capability restrictions, verification is primarily structural (type-checking and contract satisfaction) rather than arbitrary dataflow analysis.
+ *Merge.* If verification passes, the graph transformation is merged. The human approved the graph diff; the machine confirmed conformance. Routine human review of generated node implementations is not required to establish architectural or security properties — those are decided at the graph level. Spot checks of generated code for efficiency, clarity, or residual concerns remain available to reviewers who want them; the default position of the workflow is that contract-satisfying implementations are accepted without line-by-line review.

Nodes are individually testable by injecting mock capability objects and asserting output signals against input sequences. Graph-level integration tests are expressed the same way, at the graph boundary. The replay mechanism described in @sec:time doubles as regression testing infrastructure: a recorded production event log is a ready-made test suite.

== The runtime <sec:runtime>

Each node executes in a lightweight, capability-restricted sandbox (a WASM module, a Monty-style interpreter, or a BEAM-like process), with CHERI hardware where available. Critical properties:

*No ambient authority.* As described in @sec:signal-graph, a node would not be able to import libraries, access the filesystem, make network calls, or perform any side effect beyond calling methods on its injected capability objects, enforced by the absence of any mechanism rather than a policy guard.

*Defence in depth.* The type system would prevent the graph from expressing forbidden capability grants. The runtime sandbox would prevent generated code from exceeding its injected capabilities. The operating system compartmentalises processes with OS-level enforcement. On CHERI hardware, the processor prevents capability forgery at the memory level. These layers are not fully independent, because the runtime's capability injection is configured by the type system's analysis, so a type system bug could misconfigure the runtime. But they provide overlapping coverage with distinct failure modes: a sandbox escape does not help an attacker who lacks a hardware capability, and a type system error does not propagate past a correctly configured OS compartment. This is weaker than fully independent enforcement but substantially better than any single layer.

*Language agnosticism.* WASM is the natural compilation target, supporting Rust, C, C++, Go, and Python (via interpreters such as Monty). The signal graph defines component interfaces using a language-neutral type system; the implementation language is an optimisation choice made by the AI agent, or specified by performance constraints in the node's contract.

*Snapshotting and resumption.* Nodes can be paused, serialised, and resumed, enabling durable execution, time-travel debugging, and the production replay loop described in @sec:time. BEAM supports this robustly: hot code reloading and process-state inspection are native. WASM support is maturing — WASI 0.3 @wasi_030_2026 brought native async to the Component Model, which is progress on exactly the in-flight-I/O composition this needs — but component snapshotting and resumption mid-execution are not yet a solved capability. This is an implementation constraint for the Phase 1 demonstrator, not a fundamental limitation of the model.

*Evaluation strategy.* How the runtime propagates signal changes through the graph is a design choice with performance implications. A naive strategy re-evaluates every node on every input change; a differential strategy, in the tradition of differential dataflow @mcsherry_differential_2013 and DBSP @budiu_dbsp_2023, evaluates only the nodes affected by the change. The Phase 1 demonstrator targets the latter, treating the graph as a dataflow network whose nodes memoise outputs against inputs. This is consistent with the differential FRP line of work referenced in @sec:frp-brief and is what makes per-node sandboxing cost-acceptable at graph scale.

=== Performance characteristics <sec:performance>

The performance overhead of per-node sandboxed execution is not yet established for this model. For order-of-magnitude reference, published numbers and vendor documentation put WASM module instantiation for small modules at the sub-100μs range, BEAM process spawning at single-digit microseconds @armstrong_making_2003, and microVM- or Monty-style interpreter startup in the microsecond-to-millisecond range; all three achieve efficient execution once running. A typical microservice-to-microservice call over the network costs on the order of 1ms, so per-node overhead at the microsecond scale is unlikely to dominate in architectures where nodes correspond to coarse-grained components (individual services or bounded contexts). However, the cost of capability-mediated I/O, serialisation at node boundaries, and signal propagation through graphs with hundreds of nodes has not been measured at scale in this context, and overhead may be prohibitive if the graph is decomposed to the granularity of individual functions. Determining the right granularity — coarse enough for acceptable overhead, fine enough for meaningful capability isolation — is an empirical question that the Phase 1 demonstrator must answer. A rough working envelope: if each node performs on the order of 10ms of useful work, per-crossing cost below roughly 1ms keeps total overhead under about 10%, which we take as the target the Phase 1 benchmarks should establish or refute.

The proof-of-concept component tier (@sec:phase1) supplies a first, small-scale data point against that envelope. Running two nodes as WASM components under `wasmtime`, a single capability-boundary crossing — one call into a typed WIT capability interface and back, including lifting and lowering the values it carries — costs on the order of tens of microseconds, comfortably inside the envelope (well under 1ms, hence under roughly 1% overhead for a node doing 10ms of useful work). Per-node instantiation costs tens of microseconds more, and component compilation is a one-time cost of a few milliseconds amortised across invocations.

The measurement was worth making because the typed boundary is not obviously free. The tier's first implementation passed a packed `(ptr, len)` pair into linear memory and let both sides parse the bytes; the current one marshals typed records, enums, lists, and variants on every crossing, which is strictly more work per call. It did not cost the envelope: the crossing remains in the same order of magnitude. The figures should not, however, be read as a like-for-like improvement on the ones previously reported for the flat-ABI tier, because the benchmark's timing discipline was corrected at the same time — it now warms each measurement and takes the best of several rounds, where before it timed a single cold pass and so charged JIT and interpreter warm-up to whatever it measured first. That defect inflated the earlier absolute figures, and, because the per-crossing cost is derived by _differencing_ two timings, it could have inflated the crossing figure by an order of magnitude in either direction. The comparison that survives is the one that matters here: typing the boundary did not move the crossing out of its order of magnitude.

These are proof-of-concept figures from a Python host and a two-node slice, not the Phase 1 language or a production runtime at graph scale; they establish that the crossing itself is cheap and locate the fixed cost in instantiation, which instance pooling would amortise. Serialisation cost at node boundaries is a further concern; for complex types, shared-memory approaches (CHERI compartments within a single address space or zero-copy WASM memory) may be necessary to keep per-crossing overhead within this envelope.

== Security properties <sec:security>

The capability-injection model would provide security guarantees qualitatively different from those achievable by code review or runtime monitoring.

*Injection attacks.* SQL injection and command injection depend on untrusted input reaching an interpreter in executable form. In the signal graph, a SQL-executing capability would accept typed queries, not raw strings. `Untrusted<string>` could not reach it without passing through a sanitisation node that produces a typed query. In a sound realisation of the type system, the pattern would be ill-typed — rejected by the type system rather than left to convention.

*Prompt injection.* Structurally attenuated through the combination of trust annotation, capability scoping, and schema discipline described in @sec:frp-brief and @sec:concrete-graph: no well-typed wiring would connect an untrusted source to a tool-capable LLM without transiting a type that constrains the downstream payload. The strength of this guarantee depends on the schema chosen for the discharging type (a free-text field that reaches an LLM-capable node remains adversarial data even if the wrapper is non-`Untrusted`); the framework provides the enforcement substrate, disciplined schema design makes use of it.

*Supply chain attacks.* The defence operates primarily through _capability scoping_ rather than I/O denial. A library used within a pure node has no ambient authority and can be malicious with no effect, but most libraries in practice are used within nodes that _do_ hold capabilities: a database driver inside a node with a `DBHandle`, an HTTP client inside a node with a network capability. For that common case, the defence is that the capabilities are _scoped_: the `DBHandle` is bound to a single database in a specific mode; the HTTP client is scoped to a declared set of endpoints; the LLM client is scoped to a tool allowlist. A malicious library inherits only the scope of the node it inhabits, not ambient authority, and the blast radius is bounded by that scope rather than by the library's creativity. On CHERI hardware, even a library that attempts to exploit a memory safety vulnerability to escape its sandbox cannot forge a capability to memory it was not granted. If a library update introduces a new capability requirement, this appears in the graph diff as a new `with` clause — a visible, reviewable change — rather than as an implicit elevation.

*Confused deputies.* A privileged node acting on instructions derived from an unprivileged source is the classical confused-deputy pattern; the signal graph attenuates it through two mechanisms. First, capability scoping (above) limits the damage any one node's authority can do. Second, for operations whose safety depends on _which user_ initiated them, capabilities passed at the graph boundary can be bound to the calling user's credentials, such that downstream nodes operate only within that user's authorised scope. The distinction between user-level authorisation and node-level capability injection — and the precise mechanism by which the former is threaded through the latter — is a Phase 1 design obligation noted in @sec:concrete-graph and expanded in Technical Note A.

*Covert channels remain an open concern.* A node granted a permitted capability can in principle encode information into its legitimate outputs (the choice of SQL query, the timing of LLM invocations, the shape of emitted events) in ways that leak data through channels the type system does not model. The `Untrusted<T>` discipline and the noninterference properties of the underlying type system would address a subset of these, but general covert-channel elimination is a known-hard problem and is not claimed here. This is flagged as an open item in Technical Note A.

*Privilege escalation.* A node would not be able to acquire capabilities it was not given. The graph would be the complete and sole description of the system's capability distribution. On CHERI hardware, this guarantee extends to the memory level. Two caveats catalogued in Technical Note A bound this claim: a node holding a network capability can in principle acquire authority out-of-band from services the graph does not model (distributed ambient authority), and for user-scoped operations the guarantee is only as strong as the principal-binding design that Phase 1 must deliver.

= Research agenda <sec:agenda>

The research agenda is organised in three phases of increasing scope, reflecting a realistic dependency ordering. Phase 1 produces a working demonstrator; Phase 2 hardens it for meaningful deployment; Phase 3 addresses the deeper formal and hardware integration questions. The convergence of developments that makes this agenda timely — and that explains why this synthesis has not been attempted before — is discussed in @sec:why-now. We present the agenda first so that the reader encounters the "why now" argument with a concrete understanding of what the proposal actually requires.

== Phase 1: Core demonstrator <sec:phase1>

*Signal graph language and type system.* Design the capability-annotated signal graph language: its type system, its expression of trust tainting, its composition rules. The target is a language expressive enough to encode realistic system architectures while remaining amenable to visual rendering and agent manipulation. Arrowized FRP @nilsson_functional_2002 and algebraic effect systems @leijen_type_2017 are the primary formal references. A key design decision is the degree of dependent typing required: Idris 2 or Agda for full expressiveness, or a more restricted system (e.g., a Haskell-like type system with phantom types for trust levels) for tractability. The demonstrator uses the restricted system; the Phase 3 verification work may require the full one.

A stdlib-only proof-of-concept validator in the proposal's repository already exercises several Phase 1 graph-level checks on the canonical graph JSONs that underlie the figures in @sec:concrete-graph. A small type-expression parser (grammar + worked examples published as a build artifact at `dist/grammar.md`) feeds six classes of analysis: _edge type-compatibility_ (source output types, with sum-type variant resolution, must match target input types in data shape, independently of trust); _trust-lattice checking_ (trust levels form a two-point lattice `Untrusted ⊑ Trusted` with no upward coercion — `Untrusted<T>` is not a subtype of `T` — and the wiring check is flow-sensitive with respect to trust, applied uniformly to edges and to node bodies: a node may raise trust only if it is declared as a discharger with `discharges_trust: true`, as `ParseMessage` is in the `CustomerSupport` example; trust laundering is thereby a single lattice violation rather than a rule bolted on beside edge typing); _variant completeness_ (every declared sum-type output variant must have at least one consuming edge, so dead branches are visible at the graph level rather than hidden in node implementations); _capability narrowing_ (at cross-graph composition, a parent may provide a handle whose authority exceeds what the sub-graph declares — a wider `LLMClient<[...]>` tool set or a wider `DBHandle<X, read-write>` mode — while strict equality still governs every data-flow position); _intra-graph consistency_ (every declared capability is used, every boundary data input is consumed, every edge references a valid node and output variant); and _cross-graph signature matching_ (a node whose name matches another graph's name must satisfy that graph's parameter list position-by-position). The validator is not the Phase 1 language itself — it implements a first, two-point version of the flow-sensitive trust lattice discussed in Technical Note A, but not the soundness argument that well-typed wiring implies noninterference, nor the richer graded or Jif-style decentralised-label models — but it is early evidence that the graph-level analyses referenced in @sec:thesis and @sec:security are implementable with modest tooling, and it runs, alongside a unit suite exercising the parser, every check, and the runtime described below, on every build and every commit.

A companion executable runtime loads the same canonical graph JSON, instantiates each node with injected capability handles, and propagates signals along the active path. Its enforcement fidelity must be stated precisely, because the distinction is the whole point: capability confinement in the demonstrator is _host-level discipline_, not unforgeable containment. Each node receives only the handles its signature declares, and each handle exposes only the operations its type permits — a handle for `LLMClient<inference>` has no tool-calling operation at all, and one for `LLMClient<[lookup]>` refuses every other tool. This demonstrates the _shape_ of the guarantee; it does not enforce it against a node that declines to play by the object model. Enforcement against a hostile node is the business of the sandbox and hardware tiers described in @sec:runtime and @sec:phase3; the confined component tier is now prototyped (below), while hardware-backed enforcement remains future work. Within the host tier's limit, the runtime exercises two properties the proposal leans on. First, the wirings the proposal calls ill-typed are rejected at assembly time by the validator above, before any node executes. Second, an adversarial message carrying prompt-injection instructions traverses the `CustomerSupport` graph without acquiring authority: the nodes that process it hold inference-only LLM handles and so have no mechanism to act on it, and the tool-capable node receives a `ConversationContext` rather than the `Untrusted<RawMessage>` value, which is consumed at the parse boundary. The residual is exactly the one @sec:concrete-graph names — the bounded free-text field survives into that context, and what bounds the damage is the capability scope rather than the type.

A second execution tier closes the gap that host discipline leaves open. Node bodies are compiled to WASM _components_ (@sec:sandboxing) and run under `wasmtime`, and each capability kind is declared as a typed WIT interface: `LLMClient<inference>` becomes an interface offering a single inference function, `DBHandle<'knowledge-base', read>` an interface offering `lookup` and no writer, and so on. A node's `with` clause is then precisely the set of interfaces its component imports. On this tier the inference-only guarantee changes in kind: an `LLMClient<inference>` node has no tool-calling _import_, not merely no tool-calling method, and a component that declares a capability it was not granted cannot be instantiated at all. The claim is made falsifiable by a hostile-node suite — node implementations that deliberately attempt to read a file, open a socket, read the environment, or invoke an ungranted capability. The suite asserts that each attempt _succeeds_ on the host tier, recording the gap as a test rather than as prose, and _fails_ on the component tier.

Two properties of this tier are stronger than a conventional WASM sandbox, and both are consequences of the boundary being typed rather than byte-oriented.

First, _there is no ambient authority to reach for, as a property of the artifact rather than of the host's configuration_. An earlier iteration of this tier compiled nodes to `wasm32-wasip1` modules and ran them under an empty WASI context — no preopens, no sockets, no environment, no clock. That confined them, but the modules still _imported_ WASI functions (`environ_get`, `fd_write`, `path_open`); the imports were present and merely powerless, so confinement was a fact about how the host had configured the runtime, and a misconfigured host would have silently granted them. The components are instead compiled for `wasm32-unknown-unknown` and linked with no WASI adapter, so no filesystem, socket, environment, or clock function appears among their imports _at all_ — the hostile-node component imports nothing whatsoever. What was a configured absence is now a structural one, and the test suite asserts the import set directly.

Second, _the boundary cannot silently drift from the node's declared signature_. Because a component's imports are typed interfaces rather than opaque symbols, the import set the runtime will accept can be computed from the graph: the expected interfaces for each node are derived from its `with` clause in the canonical graph JSON, and checked against what the built component actually imports. A world that grants a node an interface its signature never requested fails that check rather than shipping as a quiet over-grant. The same typing closes a marshalling gap the flat byte ABI left open: a value that does not conform to an interface's declared type is refused at the boundary instead of being reinterpreted as raw bytes. The `Intent` closed set is the sharpest illustration. Under the flat ABI the node body received a label as a string and had to _check_ it against the permitted set so that adversarial text could not widen it; expressed as a WIT `enum`, the widening is not representable, the check is gone, and the property is held by the type rather than by the node's own diligence.

The two security-critical nodes run as components while the rest run on the host tier, demonstrating that the two tiers compose within one graph — the incremental-migration path of @sec:phase2 (opaque host node, then confined node). One of them, `ParseMessage`, is regenerated in Rust from the same signature and contract as its Python counterpart, a concrete instance of the code-as-compiled-artifact property: same contract, different implementation language, unchanged graph. What this tier does _not_ provide is memory-level unforgeability — a sandbox escape via a runtime defect is out of scope, and hardware-backed enforcement (CHERI) remains Phase 3 work. Nor does typing the boundary touch the residual free-text channel: the bounded question field still reaches the tool-capable node as data, and what limits the damage remains the capability scope, not the type of the boundary. Its measured overhead is reported in @sec:performance.

*Runtime prototype.* Implement a capability-restricted execution environment (initially the WASM Component Model, whose typed interfaces express capabilities directly; see @sec:sandboxing) that instantiates graph nodes with their injected capability objects and provides no ambient authority. Demonstrate that a node implementing a realistic workload (an HTTP handler with database access and LLM invocation) cannot exceed its declared capabilities, and that the security properties of @sec:security hold for a representative set of attack patterns.

*Agent tooling.* Build the AI agent workflow that takes a natural-language change description, proposes a graph transformation, generates node implementations, and submits for automated verification. This extends existing SDD tooling (OpenSpec, Kiro) to operate on typed graph artifacts rather than prose documents. The distinctive contribution is agents that reason about signal dependencies and project downstream effects of proposed changes before committing them.

*Developer experience.* Implement the visual graph editor and diff viewer. The primary human interface must make graph transformations reviewable without requiring users to read the underlying type system. Capability edge additions, trust boundary crossings, and sanitisation gaps must be visually salient.

== Phase 2: Hardening and deployment <sec:phase2>

*Shallow verification.* Develop automated tooling to confirm that AI-generated node implementations satisfy their declared contracts. Three techniques cover complementary concerns. _Property-based testing_ establishes type-level invariants across random inputs (ensuring, for example, that a parsing node never emits an ill-formed variant). _Contract testing_ checks node-local pre- and post-conditions on representative traces, giving concrete evidence for properties the static type system cannot express. _Architectural fitness functions_ @ford_building_2017 verify cross-cutting properties of the assembled graph — capability-scope constraints, trust-zone integrity, absence of forbidden wirings — that belong to the composition rather than any single node. The verification obligation is deliberately bounded: establishing type conformance, contract satisfaction, and graph-level structural invariants, not arbitrary program-property verification.

*Event log infrastructure.* Design the structured event logging that capability boundary crossings produce automatically. Define the formal conditions under which replay fidelity holds, characterise the classes of failure (primarily concurrency-dependent) that violate it, and design runtime conventions that maximise fidelity in practice.

*Migration path.* Design the incremental adoption route for existing systems. The minimal entry point is wrapping an existing service as an opaque node with a declared capability signature, a boundary that describes what the service _does_ (which databases it accesses, which external APIs it calls) without requiring any internal restructuring. This is analogous to declaring a foreign function interface: the existing service runs unchanged, but the graph now models its authority explicitly. Over time, an opaque node can be decomposed: its internal logic is extracted into sub-nodes with narrower capability signatures, progressively tightening the authority model. This incremental path must be designed with attention to intermediate states stable enough for production operation. A system that is half-migrated (some nodes fully capability-restricted, others opaque wrappers) must still provide value: opaque wrappers provide _architectural visibility_ (their authority is explicit and reviewable even before internal restructuring), while the security-by-construction guarantees of @sec:security apply only to fully migrated nodes. The migration therefore yields a graduated benefit curve: visibility at the first step, structural security earned as wrappers are decomposed. Naming this graduation honestly, rather than presenting the model as all-or-nothing, is central to the adoption story.

== Phase 3: Formal foundations and hardware <sec:phase3>

*Deep verification.* For the compilation from signal graph semantics to capability-restricted WASM, confirm that component boundaries, capability signatures, and trust annotations are preserved across the production boundary. This is a bounded correctness claim about a specific, well-defined transformation, closer in kind to CompCert @leroy_formal_2009 than to general program verification, but demanding nonetheless. Proof assistants in the tradition of Coq or Lean are the appropriate tools. The minimal set of invariants required to guarantee the security properties of @sec:security in the production runtime must be identified before the full verification work is scoped.

*CHERI integration.* Design the mapping from architectural capabilities (typed handles injected at node boundaries) to CHERI hardware capabilities at the memory level. Use CHERI's fine-grained compartmentalisation to enforce node isolation below the WASM boundary. Characterise graceful degradation on non-CHERI hardware. CHERIoT @amar_cheriot_2023 provides a reference architecture; the WASM Component Model's capability interfaces (of which WASI is the standard system-resource instance) provide the natural software interface above which CHERI enforcement is applied.

= Why now <sec:why-now>

Every element of this proposal has existed in some form for years or decades. What makes the synthesis newly practical is the convergence of four developments.

*AI agents as code authors.* The primary author of implementation code is, for the first time, an entity that does not require the affordances of text editing and has no resistance to structural constraints. This removes the primary historical obstacle to graph-based code representations.

*Lightweight sandboxing.* The WASM Component Model, Monty, and BEAM have made per-node capability-restricted execution practical at microsecond timescales. The performance overhead that historically limited capability-based systems has been substantially reduced (@sec:sandboxing).

*Capability hardware.* CHERI processors are reaching commercial availability, and RISC-V standardisation is underway. For the first time since the capability architectures of the 1970s, hardware that enforces unforgeable, bounded capabilities is becoming available for production use.

*Economic pressure.* The emergence of spec-driven development frameworks (OpenSpec @fission_openspec_2025, Kiro @aws_kiro_2025, Spec Kit @github_spec_kit_2025) is evidence that organisations adopting AI coding agents need stronger constraints than unstructured prompting provides: these tools exist because agents without structured context produce results that outstrip review capacity. Early observational data from GitClear @gitclear_ai_2024 suggests declining code quality metrics coincident with AI assistant adoption, though the methodology and causal inference are contested and the effect size remains uncertain. Whatever the precise magnitude of the quality shift, the structural argument stands independently: the faster agents write code, the more attractive formal architectural constraints on what they produce become, and the less tenable review-by-inspection is as the sole safeguard.

If these trends continue, there is a plausible path toward model-driven, capability-restricted, agent-implemented systems. The choice is between deliberate design and haphazard emergence from the collision of existing tools.

// Appendices

#set heading(numbering: none)

= Technical Note A: Open problems and known limitations <sec:open-problems>

_This note collects the technical questions that the proposal acknowledges but does not resolve. It is intended for technically specialist readers who may wish to engage with specific open problems._

*Compositionality of noninterference.* When two well-typed nodes are wired together, the composed system must inherit the noninterference properties of both. This does not follow automatically from local node typing; it requires that the trust label system be compositional in a specific sense. The result is well-established (compositionality of noninterference follows from standard results in information-flow security #cite(<sabelfeld_language-based_2003>, supplement: [§5])), but the signal graph wiring model must be shown to satisfy the conditions those results require. Adapting these results to the signal graph's wiring model is a design and research task for Phase 1, not a mechanical application. It is worth stating explicitly so that the design work does not inadvertently introduce a label system that is locally sound but fails to compose. The condition the lattice must satisfy can be stated concretely: the flow relation used by the wiring check (`required ⊑ provided`, with a node's output label a monotone function of the meet of its input labels) must be transitive, so that a path of individually well-typed edges is itself well-typed, and monotone, so that composing nodes cannot manufacture trust the parts did not have. The two-point lattice realised in the PoC (@sec:phase1) satisfies both trivially; a graded or decentralised-label successor must re-establish them, because transitivity and monotonicity are exactly what a locally-sound-but-non-compositional label system can lose. The mechanised proof that these conditions deliver noninterference is deferred to Phase 3 (@sec:phase3).

*The coercion problem in trust-annotated wiring.* As discussed in @sec:signal-graph, the trust annotation scheme requires a flow-sensitive wiring type system (beyond local node typing alone) to guarantee noninterference. The precise design (security label lattice, absence of `Untrusted<T> <: T` subtyping, or a Jif-style label system) is an open design question for Phase 1. The problem is well-understood in the information-flow literature; the contribution is adapting it to the graph wiring context. A first realisation now exists in the PoC validator (@sec:phase1): a two-point lattice `Untrusted ⊑ Trusted` in which `Untrusted<T>` is not a subtype of `T`, a flow-sensitive edge check, and discharge as the sole upward move at a declared discharger — enough to reject the laundering rewiring below as a lattice violation rather than to rely on a separate rule. What remains open is the choice among the two-point, graded, and Jif-style lattices, and the soundness argument tying well-typed wiring to noninterference.

The proof-of-concept validator (@sec:phase1) supplies a small piece of empirical support for treating trust as a lattice property rather than something that falls out of edge typing alone. Rewiring `CustomerSupport` so that untrusted input reaches the tool-capable node directly is rejected as an ordinary data-shape mismatch. The subtler "repair" — widening the tool-capable node's declared input to `Untrusted<RawMessage>` so the edge type-checks on data shape — yields a graph in which every edge is data-compatible, yet it is still rejected: the tool-capable node now raises trust, emitting a non-`Untrusted` output from an `Untrusted<_>` input without being a declared discharger, which is an upward coercion under the lattice. Crucially, this is caught by the _same_ no-upward-coercion order that governs edges, applied to the node body, not by a separate rule bolted on beside edge typing. That is what folding trust into the lattice buys: trust laundering is a wiring violation of one principled order, and an earlier design that had instead folded trust levels into ordinary subtyping — admitting `Untrusted<T> <: T` — would have accepted the widened graph with the failure invisible at the level of edge types. This is the concrete argument for the "no upward coercion" discipline above, and a caution that local edge data-compatibility is not the property to check. What the PoC does _not_ establish is soundness — that every graph well-typed under the lattice in fact satisfies noninterference; that obligation is Phase 3's (@sec:phase3).

*Replay fidelity under concurrency.* The production replay loop described in @sec:time assumes that the event log at capability boundaries is a complete and deterministic record of the system's inputs. This holds for single-threaded deterministic nodes but degrades for nodes with internal concurrency or timing dependencies. Fan-in is the inter-node instance of the same problem: where several upstream nodes feed a single consumer (as the audit node in @sec:concrete-graph), the arrival order of concurrently emitted signals is a scheduling artifact rather than a topological property, so the event log must record the realised merge order — and the runtime must reproduce it — for replay to hold across fan-in points. The formal conditions under which replay fidelity holds, and the runtime conventions that maximise it, are open questions addressed in Phase 2. Perfect replay is not the claim; rather, capability-boundary logging would provide materially better fidelity than conventional logging, and the classes of failure that violate fidelity could be characterised and managed.

*Compilation correctness scope.* The Phase 3 claim that the FRP-to-WASM compilation preserves capability signatures and trust annotations is a bounded verification obligation, but its exact scope must be defined before proof work begins. CompCert @leroy_formal_2009 is the appropriate precedent in terms of methodology, but took a decade of dedicated effort. The realistic near-term target is a mechanised proof of preservation for a simplified subset of the signal graph language, sufficient to validate the approach and identify the hard cases, rather than a full production-grade verified compiler.

*Error handling and conditional flow.* The concrete graph in @sec:concrete-graph demonstrates basic conditional routing (three-way branching from moderation, error routing from response generation). However, real systems require richer patterns: fan-out (sending the same signal to multiple consumers), error propagation chains (a failing node must produce a typed error that downstream nodes can handle), and fallback logic. Arrowized FRP provides combinators for choice and fan-out (`ArrowChoice`, `&&&`), but their integration with capability annotations and trust tainting has not been worked out. A graph language that cannot express "on payment failure, notify the user and log the error" without escaping to imperative code would not be viable. This is a Phase 1 design obligation.

*Contract incompleteness and implementation interchangeability.* The claim that node implementations can be regenerated without changing the system's meaning (@sec:thesis) holds exactly only up to the expressiveness of the behavioural contracts. Contracts are partial specifications: two implementations can both satisfy the same contract and still differ observably — in latency, in resource consumption, in which variant they emit on inputs the contract does not pin down. Regeneration can therefore change production behaviour within the slack the contract leaves, without any verification step failing. Tightening contracts narrows the slack at the cost of authoring burden and verification tractability (@sec:phase2). Characterising which classes of behavioural difference are operationally significant, and what contract discipline renders regeneration safe with respect to them, is an open problem for the Phase 2 verification work.

*Node-local state.* The proposal frames nodes as functions whose effects are mediated through declared handles, but many real components need persistent local state between invocations (session caches, rate-limit counters, accumulated aggregations). In FRP, state is modelled through feedback loops and signal accumulators; the interaction between stateful signal combinators, capability annotations, and the deterministic replay property of @sec:time has not been analysed. A node that maintains implicit internal state may violate the assumptions that enable the replay and verification claims. The Phase 1 language design must define how state is expressed in the graph — as an explicit feedback edge, as a stateful combinator, or as a capability-mediated external store — and which of these approaches preserves the security and replay properties.

*Graph-scale comprehension.* The concrete example in @sec:concrete-graph has nine nodes. Real systems will have hundreds. Navigating, debugging, and reviewing large graphs presents UX challenges familiar from visual programming and model-driven environments: clutter, loss of context, difficulty localising errors. Hierarchical decomposition (sub-graphs exposed as single nodes at a higher level, analogous to C4's zoom levels) is the expected approach, but the interaction between hierarchical abstraction, capability wiring, and trust propagation across sub-graph boundaries has not been designed. A type error deep in a sub-graph's internal wiring must produce an error message that is comprehensible at the level the developer is working at. This is a Phase 1 developer experience obligation.

*Hierarchical capability routing.* When a parent graph provisions a capability to a sub-graph (as `SupportPlatform` provisions `LLMClient<inference>` to `CustomerSupport` in @sec:concrete-graph), the sub-graph must route that capability to the specific internal nodes that require it, and only those. Three design options are visible: (i) the sub-graph exposes a flat parameter list that the parent matches by type, with internal fan-out by convention; (ii) the sub-graph exposes named capability slots that the parent binds explicitly; (iii) structural matching on capability types routes handles automatically to every matching `with` clause within the sub-graph. Each has trade-offs for composability, review clarity, and aliasing semantics; selecting one is a Phase 1 language-design obligation. The proof-of-concept runtime makes the aliasing half of that trade-off concrete, and now resolves the narrowest part of it. By default it provisions one handle per declared capability _type_ and shares it across every node whose `with` clause names that type, so two nodes each declaring `DBHandle<'knowledge-base', read>` receive the same object. For read-only handles this is harmless; for stateful, rate-limited, or revocable handles it is not, and it shows that capability _identity_, not merely capability type, must be expressible at the graph boundary — a requirement that interacts directly with the revocation question below. The runtime now makes identity expressible as an opt-in: the boundary can name distinct instances of one capability type and route each to specific nodes, so two same-typed handles carry independent state (a tested property), while any capability with no identity declared keeps the type-shared default and existing graphs are unchanged. This is deliberately the _naming_ step only. Two larger obligations remain open. First, the _surface_ for that naming — named capability slots, opaque identity tokens, or structural matching (the three options above) — is still a Phase 1 language-design choice; the demonstrator implements the smallest surface (identity supplied at the assembly boundary) and does not yet spell identity in the graph JSON or route it across sub-graph boundaries. Second, the component tier already gives each confined node a distinct instance with its own import table, so instance isolation is a natural home for per-node identity; what a full design adds is identity carried by the _type_ rather than the instance. The component model sharpens this: WIT's `resource` types give handles their own identity and lifetime at the boundary, which is the mechanism a per-node, revocable capability would be built from — but the tier as implemented models each capability kind as a plain interface, so at the confined tier capability identity is still carried by the instance rather than by the type.

*Sub-graph output aggregation.* The dual problem at the output side is no less open. When a sub-graph's internal terminal nodes emit distinct types — the `CustomerSupport` graph in @sec:concrete-graph emits `DeliveryConfirmation` from its three reply nodes and `EscalationTicket` from its escalation path — the boundary must collapse those into the single type declared by the parent (`ServiceOutcome`). Three options mirror the input-side case: (i) define the boundary output as the union of terminal types (so `ServiceOutcome = DeliveryConfirmation | EscalationTicket` is a type alias and no aggregation node is required); (ii) declare the sub-graph with named output ports that the parent wires individually, permitting a multi-output boundary; (iii) introduce an explicit aggregation node inside the sub-graph whose output is the boundary type. The first is simplest and is the working convention used by the composition example in @sec:concrete-graph; the second preserves variant information at the parent level at the cost of a richer boundary; the third makes the aggregation explicit at the cost of an extra node. The trade-offs against trust-annotation preservation, variant addressing, and encapsulation are a Phase 1 language-design obligation.

*User-level authorisation threaded through capability injection.* The confused-deputy attenuation argument in @sec:security depends on user-scoped capabilities being bound at the graph boundary and propagated to downstream nodes as part of the injected handle. The mechanism — whether capabilities carry an authenticated principal, whether they compose under delegation, and how sub-graph boundaries preserve the principal across composition — is an open Phase 1 design question. Naming this explicitly matters because the graph-level security argument leans on capabilities being both scoped to a resource _and_ bound to an authorised principal; the precise form of that binding has not been designed, and a weak binding would undermine the confused-deputy and privilege-escalation claims in @sec:security.

*Capability revocation and rotation.* Capabilities are provisioned when the graph is instantiated, but production operation sometimes requires withdrawing authority at runtime: rotating credentials, revoking a compromised handle, draining access to a deprecated store. The object-capability literature handles revocation through indirection — caretaker proxies that can be severed @miller_robust_2006. Revoking a _specific_ handle first requires a way to name it: with capabilities shared by type, there is no instance to revoke without severing every node that names the type. The per-node capability identity above supplies that prerequisite — a declared identity names one instance — and the demonstrator now builds the mechanism on it, for the host tier. An instance provisioned as _revocable_ is wrapped in a caretaker: the node holds a forwarding proxy, and a separate revoke authority — kept by the host that assembled the graph, never handed to a node — severs it, after which the node's next use of the instance fails loudly instead of exercising authority. Revocation is _targeted_ (severing one identity leaves its same-typed siblings, and the type-shared default, untouched) and _opt-in_ (an instance not declared revocable is provisioned bare, so un-revoked graphs are byte-for-byte unchanged) — both tested properties. _Rotation_ — re-pointing a live handle at a new resource behind the same identity — reuses the very same indirection and is now demonstrated alongside revocation: the caretaker forwards to a mutable _target_ that a separate rotate authority re-points, so revoke (sever the target) and rotate (replace it) are two levers on one proxy rather than two mechanisms. Rotation is guarded to a same-kind replacement (so the surface the node holds cannot change kind underneath it), is again targeted and opt-in, and grants its authority independently of revocation — all tested. This narrows the open question to its remaining parts. The _graph-transformation / redeployment_ form of both operations is undesigned. Enforcement of revocation, however, now reaches the confined tier: a sandboxed node reaches its capabilities through typed WIT host functions backed by the very same caretaker, so severing that caretaker makes the guest's next capability crossing fail at the boundary — and that is unforgeable there, because the confined node's import set is its whole world and it has no other path to the resource. (This holds at the WIT boundary, not the memory level, where CHERI remains the named follow-up; and it is revocation that was carried across — sandbox-tier _rotation_, re-pointing over the same boundary, is the natural next step.) On the host tier the caveat is unchanged: severing or re-pointing binds the handle's authority but not a node that reaches around the object model. This is Phase 2 work adjacent to the migration and operations story.

*Covert channels through permitted capabilities.* As noted in @sec:security, a node granted a permitted capability can in principle encode information into its legitimate outputs in ways that leak data through channels the type system does not model (timing, query shapes, event ordering). The `Untrusted<T>` discipline addresses explicit data flow; noninterference results handle the class of flows the type system observes. General covert-channel elimination is a known-hard problem and outside the claims of this proposal. The pragmatic target for Phase 2 is characterisation: naming which channels the model closes, which it narrows, and which remain out of scope.

*Graph evolution and signature compatibility.* When a node's type signature changes, downstream consumers may break. The graph language must define compatibility rules for signature evolution (additive changes, narrowing of capability requirements, widening of output types) and support versioned interfaces at sub-graph boundaries to enable independent team ownership. This is a standard API evolution problem, but its interaction with capability wiring and trust annotations adds constraints that the Phase 1 design must address.

*Type system soundness.* The security properties of @sec:security depend entirely on the type system being sound: every well-typed graph must satisfy noninterference and capability confinement. A soundness bug in the type system would propagate to every layer of the defence-in-depth stack (@sec:runtime), since the runtime's capability injection is configured by the type system's analysis. Phase 1 should include property-based testing of the type system (random graph generation with expected type errors, fuzzing of the wiring checker) as a lightweight validation before Phase 3's mechanised soundness proof for the core calculus. A first slice of this already runs: the PoC test suite generates random trust labellings over the two-point lattice and asserts that an edge is rejected on trust grounds exactly when it is an upward coercion — a small worked instance of the random-graph-with-expected-error discipline this item calls for, not a substitute for it.

*Distributed ambient authority.* The signal graph model controls capability flow within a deployment, but a node wired to a network capability handle can, in principle, communicate with any reachable service, potentially acquiring capabilities out-of-band that the graph does not model. The E language @miller_robust_2006 and Agoric's Hardened JavaScript address this through reference-based communication discipline; our proposal inherits the same open question for the distributed case. Scoping the system to a single deployment boundary for Phase 1 and Phase 2 is the pragmatic approach; the distributed extension is a later-phase research question.

*Type system evolution.* The graph language's own type system will need to evolve: new trust levels, new capability kinds, refined subtyping rules. This is distinct from the graph evolution problem above (which addresses node signature changes within a fixed type system). Migration of existing graphs across type system versions, and the preservation of verified properties across such migrations, is an open problem that the Phase 1 design should anticipate, drawing on established approaches to language versioning and gradual typing.

= Annex B: Areas for collaboration <sec:collaboration>

_This proposal is an invitation. The synthesis it describes spans several domains that no single team is likely to cover. This annex identifies the expertise each phase requires, as a guide for potential collaborators._

== Phase 1: core demonstrator

- *Type theory and functional programming:* algebraic type systems, arrowized FRP, algebraic effect systems. Experience with Haskell, Idris, or Agda. The signal graph's type system is the foundation; getting it wrong here propagates to every later phase.
- *Systems programming:* WASM Component Model and WASI toolchains, capability-based I/O models, runtime implementation in Rust or C++.
- *AI agent tooling:* structured agent workflows, tool use, MCP (Model Context Protocol).
- *Developer experience design:* visual graph editors, diff viewers, reviewer cognitive load. This is as important as the formal foundations.

== Phase 2: hardening

- *Formal methods:* property-based testing (QuickCheck, Hypothesis), contract testing, lightweight specification (TLA+, Alloy).
- *Distributed systems and observability:* structured logging, distributed tracing, causal ordering @lamport_time_1978, OpenTelemetry.
- *Security engineering:* capability-based security, supply chain threat models, prompt injection as an attack class.

== Phase 3: formal foundations

- *Proof assistants:* Coq or Lean 4 at theorem-proving level, for the bounded compilation correctness claim.
- *Computer architecture:* CHERI instruction set architecture (ISA) extensions, CHERIoT hardware-software co-design. The Cambridge CHERI group is the primary external knowledge source.

// Bibliography
#bibliography("citations.bib", style: "ieee")
