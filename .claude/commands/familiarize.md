Please familiarize yourself with this project by reading:

1. `AGENTS.md` (project context and editing guidelines — includes the corpus structure and the current state of the toolchain)
2. `papers/README.md` (how the corpus is organised: which papers are living, which are frozen)
3. `docs/ROADMAP.md` (what is being built next, and the "state as of" header describing the present)
4. The two living papers, as needed for the task at hand: `papers/02-demonstrator/proposal.typ` (the systems paper) and `papers/03-method/proposal.typ` (the method paper). Paper 1 (`papers/01-vision/`) is frozen — read it only for cross-reference, never edit it.
5. `citations.bib` (shared bibliography)
6. `README.md` (build setup)

Also glance at `git log --oneline -10` to see recent changes, but note that your next task may not be related to these.

Do not run any commands beyond git status/show/log and file reads. Let me know when you're ready and if you have any questions or concerns about the current state of the repo.
