"""AI-generated node implementations.

Modules here were generated by an AI agent from a node's signature + contract,
demonstrating the proposal's "code as compiled artifact" property: the runtime
treats node bodies as generated artifacts derived from the graph. Each module
ships alongside the prompt used to generate it (`*.prompt.md`).
"""
