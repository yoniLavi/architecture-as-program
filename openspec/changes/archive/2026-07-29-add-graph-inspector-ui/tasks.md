## 1. Execution API (Python side, UI-independent)

- [x] 1.1 Add a thin HTTP entry point in `poc/` (serving canonical graph JSON unmodified; POST run → assemble/execute → trace or validator rejection with reason class); dependencies stay in the `poc` group
- [x] 1.2 pytest contract tests for the API: graph serving byte-parity with `graphs/*.json`, trace response schema-validity, rejection surfacing — no Node required

## 2. UI scaffold

- [x] 2.1 Scaffold the Next.js app under `ui/` with its own lockfile; add opt-in `make ui` / `make ui-check` targets; verify `make build` on a Node-less PATH is unchanged
- [x] 2.2 Graph view rendering served canonical JSON (client-side layout, e.g. React Flow/ELK); node/edge detail panels showing signatures, `with` clauses, instance names, trust annotations from the parsed source

## 3. Execution and trace overlay

- [x] 3.1 Run trigger with input + tier-configuration selection; render the returned trace: per-node tier badges, edge trust labels, crossings with instance names, nested sub-graph expansion
- [x] 3.2 Validator-rejection display (reason class shown on the graph where the check failed)
- [x] 3.3 Guided prompt-injection walkthrough: taint path highlighted to the discharge node, host-vs-confined comparison view

## 4. Verification of paper-facing claims

- [x] 4.1 End-to-end check (Playwright or equivalent, inside `ui/`, opt-in) covering each behavior the paper will assert: canonical rendering, run→trace, taint visibility, tier contrast
- [x] 4.2 Decide hosting/screenshot story for the paper (static screenshots generated by the e2e run, so figures regenerate from the artifact rather than being pasted) — decided: the e2e run rewrites committed `ui/screenshots/*.png` (production build, no dev chrome), which `make build` copies into `dist/ui/` with plain `cp`; no hosted demo (outward-facing, would be a separate decision)

## 5. Paper 2 updates

- [x] 5.1 §3: report the inspector (architecture: pure consumer of graphs + traces; server-side execution) with any figures generated, not hand-made
- [x] 5.2 §5: move the tooling/visual-editor prediction to partial with the inspector-not-editor restriction in the same passage
- [x] 5.3 §7: record UI graph authoring (validator-in-the-loop editing) as the open next step
- [x] 5.4 `make build` green without Node; full suite green with the `poc` group
