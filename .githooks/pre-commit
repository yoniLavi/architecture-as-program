#!/bin/sh
# Pre-commit hook: build outputs and check for unused citations

set -e

make build

# Check for unused bib entries (warnings only, non-blocking)
python3 "$(git rev-parse --show-toplevel)/scripts/check-citations.py"

echo "Pre-commit checks passed."
